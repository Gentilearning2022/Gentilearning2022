# write 3 different variables with the input being a number 
number1 = int(input("Enter the first number: "))
number2 = int(input("Enter the second number: "))
number3 = int(input("Enter the third number: "))

# assume the the largest number is number1
largest_number = number1

#write an if statement to verify if number1 is really the largest number
if number2 > largest_number:
    largest_number = number2

# create another if statement for number 3
if number3 > largest_number:
    largest_number = number3
    
# print the result for the largest number
print("The largest_number is:", largest_number)
